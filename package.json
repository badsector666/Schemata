{
  "name": "schema-crud-worker",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create KV_STORE",
    "kv:create-preview": "wrangler kv namespace create KV_STORE --preview"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
